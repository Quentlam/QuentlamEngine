# QuentlamEngine 开发清单

## 目标
把 QuentlamEngine 发展成一个能够支持“星露谷”类型 2D 生活模拟游戏的通用引擎框架。

## 一、项目定位

QuentlamEngine 当前已经具备较完整的底层能力，包括：
- 2D / 3D 渲染
- ECS 与场景管理
- 2D 物理与碰撞
- 资源加载与线程池
- 编辑器与调试基础

但要支撑“星露谷”类型的项目，还需要补齐的是“游戏框架层”与“内容生产层”，而不是继续堆底层图形能力。

## 二、必须补齐的核心模块

### 1. `WorldGridModule`
职责：
- 负责格子地图、分层、区域、碰撞查询、地图切换。
- 负责世界坐标与格子坐标之间的转换。
- 负责地图触发器、传送门、区域边界与局部加载。

输出：
- `TileMap`
- `Region`
- `Portal`
- `Chunk`
- `GridQuery`

### 2. `AnimationModule`
职责：
- 负责 2D 精灵动画、方向状态、播放控制、事件帧。
- 负责角色、工具、作物、动物、门、特效等统一动画播放。

输出：
- `AnimationClip`
- `Animator`
- `SpriteAtlasBinding`
- `AnimationStateMachine`

### 3. `InteractionModule`
职责：
- 负责玩家与世界的交互入口。
- 负责格子选择、实体选择、触发器执行、上下文提示。

输出：
- `Interactable`
- `InteractionQuery`
- `ActionDispatcher`
- `InteractionPrompt`

### 4. `InventoryModule`
职责：
- 负责物品、堆叠、工具、容器、装备、交易、配方。
- 负责物品实例状态与定义数据分离。

输出：
- `ItemDef`
- `ItemStack`
- `InventorySlot`
- `Container`
- `Recipe`
- `ShopStock`

### 5. `SimulationModule`
职责：
- 负责时间、日历、天气、季节、日切换。
- 负责每日结算、事件广播、规则触发。

输出：
- `GameClock`
- `Calendar`
- `WeatherState`
- `SeasonRule`
- `DailyEventBus`

### 6. `NpcModule`
职责：
- 负责 NPC 日程、路径、关系、对话入口和状态记忆。
- 负责 NPC 的地点切换、行为切换与条件判断。

输出：
- `NpcSchedule`
- `PathGoal`
- `RelationshipState`
- `NpcMood`

### 7. `QuestEventModule`
职责：
- 负责任务、事件树、过场、日记、邮件。
- 负责条件触发与分支推进。

输出：
- `EventGraph`
- `QuestState`
- `CutsceneStep`
- `MailItem`
- `EventCondition`

### 8. `PersistenceModule`
职责：
- 负责存档、读档、版本迁移、局部保存、完整性校验。
- 负责把世界状态恢复为可运行的完整快照。

输出：
- `SaveSlot`
- `SaveSchema`
- `MigrationStep`
- `StateSerializer`
- `SaveValidator`

### 9. `UIGameModule`
职责：
- 负责运行时 UI、焦点、窗口栈、输入上下文。
- 负责游戏内界面与编辑器界面分离。

输出：
- `ScreenStack`
- `WidgetTree`
- `FocusManager`
- `InputContext`
- `CursorState`

### 10. `ContentPipelineModule`
职责：
- 负责资源导入、图集、校验、热加载、内容打包。
- 负责把编辑资源转换为运行时资源。

输出：
- `Importer`
- `AtlasPacker`
- `ContentManifest`
- `HotReloadWatcher`
- `AssetValidator`

### 11. `AudioModule`
职责：
- 负责音乐、音效、环境音、分层播放、淡入淡出。
- 负责事件驱动的音频播放控制。

输出：
- `AudioBus`
- `MusicController`
- `SfxEmitter`
- `AmbientZone`
- `AudioMixerState`

### 12. `LocalizationModule`
职责：
- 负责字符串表、语言切换、字体回退、文本格式化。
- 负责 UI 文本与内容文本的统一管理。

输出：
- `StringTable`
- `LocalizedText`
- `FontFallbackSet`
- `TextFormatter`

### 13. `DebugToolsModule`
职责：
- 负责地图检查、碰撞可视化、实体拾取、存档诊断。
- 负责开发期叠层、调试命令与校验输出。

输出：
- `MapInspector`
- `ColliderOverlay`
- `EntityPicker`
- `SaveValidator`
- `DebugConsole`

### 14. `ModdingModule`
职责：
- 负责内容包、扩展点、脚本桥接、加载顺序。
- 负责模组的发现、解析与安全加载。

输出：
- `ModLoader`
- `ContentPack`
- `ScriptBridge`
- `LoadOrderResolver`
- `ModManifest`

## 三、推荐目录结构

```text
QuentlamEngine/
  src/Quentlam/
    World/
    Gameplay/
    UI/
    Audio/
    Content/
    Persistence/
    DebugTools/
    Modding/
```

## 四、开发顺序

### 第一阶段：打地基
1. `WorldGridModule`
2. `TileMap` 数据结构
3. `AnimationModule`
4. `InteractionModule`
5. `PersistenceModule`

### 第二阶段：形成可玩性
1. `InventoryModule`
2. `SimulationModule`
3. `UIGameModule`
4. `NpcModule`
5. `QuestEventModule`

### 第三阶段：内容生产能力
1. `ContentPipelineModule`
2. `LocalizationModule`
3. `AudioModule`
4. `DebugToolsModule`
5. `ModdingModule`

## 五、最小可玩版本目标

做到以下内容后，才算进入“可玩原型”阶段：
- 玩家可以在网格地图上移动
- 玩家可以与地块或物体交互
- 玩家可以打开背包并拾取物品
- 游戏内有时间推进和天气变化
- 游戏可以存档和读档
- 至少有一个 NPC 和一条事件链

## 六、技术原则

- 玩法层必须数据驱动。
- 地图、物品、NPC、事件要尽量配置化。
- 存档必须从一开始就设计版本迁移。
- 编辑器要为内容生产服务，而不是只用于摆放实体。
- 渲染、物理、逻辑、UI 要职责分离。
- 优先做通用框架，再做具体玩法。

## 七、下一步建议

1. 先实现 `WorldGridModule` 和 `TileMap`。
2. 再实现 `AnimationModule` 和 `InteractionModule`。
3. 然后补 `PersistenceModule`。
4. 最后再扩展 UI、NPC、任务、内容管线。
