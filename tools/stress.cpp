#include <iostream>
#include <windows.h>

int main()
{
    std::cout << "Stress testing play/stop mechanism..." << std::endl;
    // We will just use the play-button-regression.ps1 but run it 10 times
    return 0;
}